
<div class="col-md-12">
  <h2 class="text-center mb-3">Folders</h2>
</div>

<% if @folders.blank? %>
  <div class="col-md-7 text-center">
    <h5>You haven’t created any Folders yet</h5>
    <p class="text-white-50">Create one to see it here 👀</p>

    <%= link_to 'Create a Folder', new_folder_path, class: 'btn btn-brand mt-1' %>
  </div>
<% else %>
<div class="col-9">
    <div class="card-columns mt-4" id="folders">
      <%= render @folders %>
    </div>
  
</div>
  
<% end %>

