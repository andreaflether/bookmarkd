toastr.success("Tweet added to <b><%= j @folder.name %></b>!");

$("<%= j render tweet %>").prependTo($('#tweets-container')).fadeIn('slow');
$('#tweetUrl').val('');

$('#tweetsCount').text("<%= j pluralize(@folder.tweets.count, 'tweet') %>")

$('#no-tweets').remove();
