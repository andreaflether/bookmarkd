toastr.success("Tweet added to <b><%= j @folder.name %></b>!");

$('#tweetUrl').val('');

$('#no-tweets').remove();

$("#pagination").load(location.href+" #pagination>*","");

$('#tweetsCount').html("<%= j page_entries_info bookmarks %>")

$("<%= j render bookmark %>").prependTo($('#tweets-container')).fadeIn('slow');

$('#tweets-container').empty();
$("<%= j render bookmarks %>").appendTo($('#tweets-container')).fadeIn('slow');

