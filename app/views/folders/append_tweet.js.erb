toastr.success("<%= t('controllers.folders.tweet.added', folder: @folder.name).html_safe %>");

$('#tweetUrl').val('');

$('#no-tweets').remove();

$('#tweetsCount').html("<%= pluralize_bookmarks_count(@folder) %>")

$("<%= j render bookmark %>").prependTo($('#tweets-container')).fadeIn('slow');

initializeTippy();