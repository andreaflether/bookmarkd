$('#tweets-container').append('<%= j render @bookmarks %>');

$('#pagination').replaceWith('<%= j render 'folders/shared/pagination' %>')

<% unless @bookmarks.has_next? %>
  $(window).off('scroll');
  $('#pagination').remove();
<% end %>

initializeTippy();